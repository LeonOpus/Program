# Day 05 - Embedding 与语义相似度

**日期：** 2026-05-07

---

## 学习目标

- 理解 Embedding 的作用：把文本映射到高维向量空间
- 掌握 `sentence-transformers` 的基本用法
- 通过余弦相似度体验语义距离的含义
- 为后续 RAG 检索阶段打基础

---

## 实验结果

> 运行 `conda activate rag-learn && python scripts/embedding_lab.py` 后填写

**向量维度：**

```
# 粘贴输出，示例格式：
# 句1: shape=(384,)  | vLLM 是一个高吞吐量...
```

**余弦相似度结果：**

| 句对 | 相似度 | 备注 |
|------|--------|------|
| 句1 vs 句2（英文同义） | | |
| 句1 vs 句3（中文近义） | | |
| 句1 vs 句4（中文相关） | | |
| 句1 vs 句5（无关内容） | | |

**观察到的规律：**

- 与 vLLM 相关的句子相似度在什么范围？
- 无关句子（句5）的相似度明显低多少？
- 中英文语义相近的句子，多语言模型能正确对齐吗？

---

## 当前理解

> 用自己的话解释，不看资料能说清楚最好

- **Embedding 是什么：**

- **余弦相似度为什么适合比较向量：**

- **为什么用多语言模型（multilingual）而不是纯英文模型：**

- **PagedAttention 解决了什么问题：**

---

## 下一步问题

- [ ] 不同模型（如 `bge-m3`、`text-embedding-ada-002`）维度和效果有何区别？
- [ ] 如何把向量批量存入向量数据库（Chroma / FAISS）？
- [ ] RAG 检索时 Top-K 怎么选，相似度阈值怎么设定？
- [ ] Embedding 模型和生成模型（LLM）在推理链路中各自的角色是什么？
